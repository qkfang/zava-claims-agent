# Loss Adjuster — generated workbook output

This directory holds Excel workbooks produced at runtime by the
`generateClaimExcel` MCP tool (see `Mcp/LossAdjusterMcpTools.cs`). Files
are served from `/loss-adjuster/download/{fileName}` by the download
endpoint registered in `Program.cs`.

Files generated here are intentionally not tracked.
